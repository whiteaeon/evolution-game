# dependencies & build output
node_modules/
dist/

# Vite / tooling caches
.vite/
*.tsbuildinfo

# agent-loop runtime memory (the seed backlog IS tracked; these are per-run state)
agent-loop/state/results/
agent-loop/state/ledger.jsonl
agent-loop/state/loop.lock

# os / editor
Thumbs.db
.DS_Store

# builder's per-turn report (read from disk by the loop; never needs committing)
AGENT_REPORT.json
