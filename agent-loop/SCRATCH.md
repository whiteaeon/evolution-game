# Agent-loop scratch

This tracked file is the safe target the `-Mock` dry-run builder appends to, so a
wiring self-check can produce a real commit + diff without ever touching the game
source. Real builder turns do not use this file.
